func fib(num: int) : int {
    if (num == 0) {
        return 0;
    } else if (num == 1) {
        return 1;
    } else {
        return fib(num - 1) + fib(num - 2);
    }
}

func main () {
    var x : int = 3;
    print(x);
    print(fib(x));
    
    return 3;
}